# this is used to not exceed the usage of the resources to the namespaces.
apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute-resource-quota
spec:
  hard:
    cpu: "200m"
    memory: "500Mi"
    pods: "6"
